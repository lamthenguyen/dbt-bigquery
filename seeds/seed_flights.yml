version: 2

sources:
  - name: flights
    loader: Cloud Storage
    tables:
      - name: flights
        external:
          location: "gs://etl-airflow-bigquey/flights.csv"
          options:
            format: csv
            skip_leading_rows: 1
